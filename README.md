# Agent Workflow Engine (Backend)

## Quickstart (backend)

1. Create & activate Python venv:
   ```powershell
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
   pip install -r requirements.txt
